[project]
name = "mijia-skill"
version = "1.0.0"
description = "OpenClaw skill for controlling Xiaomi Mijia smart home devices"
requires-python = ">=3.8"
dependencies = [
    "mijiaapi>=3.0.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
